What is Git?
Git is a version control system used to track changes in code and other files. It allows us to create checkpoints called commits and collaborate with other developers.

What is a repository?
it is a project that git is tracking.
What is a commit?
it is a checkpoint .

What is the staging area?
the area where files are stored .without stagging area git could commit everything 

Why do we use .gitignore?
to tell git which files to skip and leave untracked.